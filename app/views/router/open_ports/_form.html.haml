-if @open_port.errors.any?
  #error_explanation
    %h2= "#{pluralize(@open_port.errors.count, "error")} prohibited this open_port from being saved:"
    %ul
      - @open_port.errors.full_messages.each do |msg|
        %li= msg

.field
  = f.label :port
  = f.text_field :port
.field
  = f.label :protocol
  = f.select :protocol, all_protocols
.actions
  = f.submit 'Save'
