%h1= _('Editing Tc Classid')

= form_for :tc_classid, :url => router_tc_classid_path(@tc_classid), :html => {:method => :put} do |f|
  = render 'form', :f => f

= link_to 'Back', router_tc_classids_path
