-if @forward_port.errors.any?
  #error_explanation
    %h2= "#{pluralize(@forward_port.errors.count, "error")} prohibited this forward_port from being saved:"
    %ul
      - @forward_port.errors.full_messages.each do |msg|
        %li= msg

.field
  = f.label :port
  = f.text_field :port
.field
  = f.label :protocol
  = f.text_field :protocol
.actions
  = f.submit 'Save'
